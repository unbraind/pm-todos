import type { ItemFormat, RuntimeSchemaSettings } from "../../types/index.js";
export interface ItemFormatMigrationResult {
    target_format: ItemFormat;
    scanned: number;
    migrated: string[];
    removed: string[];
    warnings: string[];
}
export declare function migrateItemFilesToFormat(pmRoot: string, targetFormat: ItemFormat, op?: string, typeToFolder?: Record<string, string>, schema?: RuntimeSchemaSettings): Promise<ItemFormatMigrationResult>;
