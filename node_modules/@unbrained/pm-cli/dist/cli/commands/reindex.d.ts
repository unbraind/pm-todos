import type { GlobalOptions } from "../../core/shared/command-types.js";
export interface ReindexOptions {
    mode?: string;
    progress?: boolean;
}
export interface ReindexResult {
    ok: boolean;
    mode: "keyword" | "semantic" | "hybrid";
    total_items: number;
    artifacts: {
        manifest: string;
        embeddings: string;
    };
    warnings: string[];
    generated_at: string;
}
export declare function runReindex(options: ReindexOptions, global: GlobalOptions): Promise<ReindexResult>;
