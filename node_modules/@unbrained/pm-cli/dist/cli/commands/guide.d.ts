import type { GlobalOptions } from "../../core/shared/command-types.js";
import { type GuideTopicDefinition } from "../guide-topics.js";
export declare const GUIDE_OUTPUT_VALUES: readonly ["markdown", "toon", "json"];
export type GuideOutputFormat = (typeof GUIDE_OUTPUT_VALUES)[number];
export declare const GUIDE_DEPTH_VALUES: readonly ["brief", "standard", "deep"];
export type GuideDepth = (typeof GUIDE_DEPTH_VALUES)[number];
export interface GuideOptions {
    topic?: string;
    list?: boolean;
    format?: string;
    depth?: string;
    [key: string]: unknown;
}
export interface GuideDocRender {
    path: string;
    purpose: string;
    optional: boolean;
    exists: boolean;
    line_count: number | null;
    content_mode: "none" | "excerpt" | "full";
    content: string | null;
    truncated: boolean;
}
export interface GuideIndexResult {
    output_default: "toon";
    mode: "index";
    depth: GuideDepth;
    topics: Array<{
        id: string;
        aliases: string[];
        title: string;
        summary: string;
        intent: string;
        quick_commands: string[];
        docs: Array<{
            path: string;
            purpose: string;
        }>;
        related: string[];
    }>;
    suggested_next_steps: string[];
}
export interface GuideTopicResult {
    output_default: "toon";
    mode: "topic";
    depth: GuideDepth;
    requested_topic: string;
    topic: GuideTopicDefinition;
    docs: GuideDocRender[];
    warnings: string[];
}
export type GuideResult = GuideIndexResult | GuideTopicResult;
export declare function resolveGuideOutputFormat(options: GuideOptions, global: GlobalOptions): GuideOutputFormat;
export declare function runGuide(options: GuideOptions, global: GlobalOptions): Promise<GuideResult>;
export declare function renderGuideMarkdown(result: GuideResult): string;
