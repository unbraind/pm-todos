import type { GlobalOptions } from "../../core/shared/command-types.js";
import type { LinkedDoc } from "../../types/index.js";
export interface DocsCommandOptions {
    add?: string[];
    addGlob?: string[];
    remove?: string[];
    migrate?: string[];
    validatePaths?: boolean;
    audit?: boolean;
    author?: string;
    message?: string;
    force?: boolean;
}
interface LinkedPathValidation {
    checked: number;
    existing_files: string[];
    missing_paths: string[];
    non_file_paths: string[];
}
interface LinkedPathAuditEntry {
    path: string;
    linked_by_count: number;
    linked_item_ids: string[];
}
export interface DocsResult {
    id: string;
    docs: LinkedDoc[];
    changed: boolean;
    count: number;
    migrations_applied?: number;
    validation?: LinkedPathValidation;
    audit?: LinkedPathAuditEntry[];
}
export declare function runDocs(id: string, options: DocsCommandOptions, global: GlobalOptions): Promise<DocsResult>;
export {};
