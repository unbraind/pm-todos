import type { GlobalOptions } from "../../core/shared/command-types.js";
import type { GovernancePreset, PmSettings } from "../../types/index.js";
export interface InitResult {
    ok: boolean;
    path: string;
    settings: PmSettings;
    created_dirs: string[];
    warnings: string[];
    governance_preset: GovernancePreset;
    wizard_used: boolean;
}
export interface InitCommandOptions {
    preset?: string;
}
export declare function runInit(prefixArg: string | undefined, global: GlobalOptions, options?: InitCommandOptions): Promise<InitResult>;
