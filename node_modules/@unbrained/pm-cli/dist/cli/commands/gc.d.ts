import type { GlobalOptions } from "../../core/shared/command-types.js";
declare const GC_SCOPE_VALUES: readonly ["index", "embeddings", "runtime"];
type GcScope = (typeof GC_SCOPE_VALUES)[number];
export interface GcCommandOptions {
    dryRun?: boolean;
    scope?: string[];
}
export interface GcResult {
    ok: boolean;
    dry_run: boolean;
    scope: GcScope[];
    removed: string[];
    retained: string[];
    warnings: string[];
    guidance: string[];
    generated_at: string;
}
export declare function runGc(global: GlobalOptions, options?: GcCommandOptions): Promise<GcResult>;
export {};
